# Integer codes for the random-effect covariance `re_struct`

Stable across versions; the C++ density dispatches on these. Covariance
structures: `us` (IID), `rw` (random walk), `ar1` (first-order
autoregressive).

## Usage

``` r
LINKAGE_STRUCT_CODES
```
