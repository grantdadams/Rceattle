# Map an `index_label` to the bin side ("age" left, "length" right)

Map an `index_label` to the bin side ("age" left, "length" right)

## Usage

``` r
.osa_bin_side(index_label)
```

## Arguments

- index_label:

  Character vector (`"age"`, `"length"`, or `NA`).
