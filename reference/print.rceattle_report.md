# Print method for a set of assessment reporting tables

Print method for a set of assessment reporting tables

## Usage

``` r
# S3 method for class 'rceattle_report'
print(x, ...)
```

## Arguments

- x:

  An `"rceattle_report"` from
  [`report_tables()`](https://grantdadams.github.io/Rceattle/reference/report_tables.md).

- ...:

  Currently unused.

## Value

`x`, invisibly.
