% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-linkage_encode.R
\name{encode_linkage_for_tmb}
\alias{encode_linkage_for_tmb}
\title{Encode a linkage table into TMB-friendly parallel vectors}
\usage{
encode_linkage_for_tmb(table, X)
}
\arguments{
\item{table}{an \code{Rceattle_linkage_table} (typically the output of
\code{\link[=pool_linkages]{pool_linkages()}}).}

\item{X}{the global design matrix from \code{\link[=pool_linkages]{pool_linkages()}} (passed
through unchanged so callers can stash it alongside).}
}
\value{
A named list with components:
\describe{
\item{n_linkage}{\code{integer(1)}. Number of coefficients
(\code{nrow(table)}).}
\item{linkage_process, linkage_param, linkage_species,
linkage_sex, linkage_age_bin, linkage_fleet, linkage_X_col, linkage_link,
linkage_re_index, linkage_prior_family}{Parallel \code{integer(n_linkage)}
vectors. \code{linkage_re_index} is \code{-1} on fixed rows, else the 0-based
\code{beta_linkage_re} slot.}
\item{linkage_prior_p1, linkage_prior_p2}{Parallel
\code{numeric(n_linkage)} vectors.}
\item{linkage_X}{The design matrix as passed in.}
\item{linkage_design_names}{\code{character} colnames of \code{linkage_X}.}
}
}
\description{
Encode a linkage table into TMB-friendly parallel vectors
}
\keyword{internal}
