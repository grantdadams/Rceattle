% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7-plot_diagnostics.R
\name{plot_catchability}
\alias{plot_catchability}
\title{Survey catchability over time}
\usage{
plot_catchability(
  Rceattle,
  file = NULL,
  model_names = NULL,
  species = NULL,
  spnames = NULL,
  mse = FALSE,
  minyr = NULL,
  maxyr = NULL,
  incl_mean = FALSE,
  log = FALSE,
  line_col = NULL,
  lwd = 3,
  lty = 1,
  width = 7,
  height = 6.5,
  right_adj = 0,
  top_adj = 0.05,
  single.plots = FALSE
)
}
\arguments{
\item{Rceattle}{A single \code{\link[=fit_mod]{fit_mod()}} object or a list of them (overlaid).}

\item{file}{Optional file stem; the figure is written to
\verb{<file>_<suffix>.png} if given.}

\item{model_names}{Legend labels for the models.}

\item{species}{Species to include, as indices (\code{c(1, 3)}), names, a logical
mask, or \code{"all"}. Default \code{NULL} plots every species. Species \strong{labels}
belong in \code{spnames}; a character \code{species} that matches no species name is
read as labels for back-compatibility.}

\item{spnames}{Species labels, length \code{nspp}. Default: the model's own.}

\item{mse}{Is \code{Rceattle} an MSE object? Its operating models are drawn.}

\item{minyr, maxyr}{First / last year to plot.}

\item{incl_mean}{Add a horizontal line at the hindcast mean of each series.}

\item{log}{Plot catchability on the log scale.}

\item{line_col}{Line colours; names, hex, or base-graphics integers. \code{NULL}
uses the colorblind-safe Okabe-Ito palette. Applied to whichever variable
the figure separates by colour, in legend order. Too few colours are
recycled, with a warning.}

\item{lwd}{Line width on the base-graphics scale: the default \code{3} renders as
a standard-weight ggplot line. A vector varies it across series.}

\item{lty}{Line type. A vector varies it across the levels of whatever the
figure separates by line type.}

\item{width, height}{Saved figure size in inches.}

\item{right_adj}{Ignored. Base-graphics leftover: the figure widened its
right margin to fit the legend. Set margins on the returned ggplot instead.}

\item{top_adj}{Ignored. Base-graphics leftover; see \code{right_adj}.}

\item{single.plots}{Ignored. Base-graphics leftover: one device per panel.
The ggplot facets instead.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the fitted survey catchability \code{q} by year, faceted by fleet. Every
fleet carrying \code{index_data} is drawn, a fishery with a CPUE series as much as
a survey.
}
\details{
The value plotted is the realized catchability the model scaled the survey by,
whichever route produced it: an estimated or fixed mean, annual deviations
under \code{Time_varying_q}, an environmental regression (\code{Catchability = "Environmental"}), a \code{q} linkage such as \code{rw(1 | Year)} or \code{ar1(1 | Year)}, or
the closed-form value under \code{"Analytical"} / \code{"AnalyticalArith"}. A fleet with
a time-invariant catchability draws a flat line, which is the honest picture
of a constant \code{q} rather than an empty panel.

\strong{Hindcast years only.} The model carries catchability over the hindcast and
does not project it, so the series stops at \code{endyr} regardless of the
projection horizon. There is no \code{incl_proj}.

\code{line_col} and \code{lty} separate the \strong{models}, so overlaying several fits
compares their catchability series panel by panel.
}
\examples{
\donttest{
data(BS2017SS)
fit <- Rceattle::fit_mod(data_list = BS2017SS, msmMode = 0)
plot_catchability(fit)
plot_catchability(fit, log = TRUE)
}
}
