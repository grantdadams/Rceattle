% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7-plot_comp.R
\name{plot_diet_comp2}
\alias{plot_diet_comp2}
\title{Plot diet composition fits (aggregation-aware)}
\usage{
plot_diet_comp2(Rceattle, file = NULL, species = NULL)
}
\arguments{
\item{Rceattle}{A single Rceattle model object.}

\item{file}{Optional file-name prefix for saved figures.}

\item{species}{Optional species names for the legend.}
}
\value{
Invisibly returns a list of the printed plot objects.
}
\description{
Diagnostic plots for diet-composition fits that adapt to how each
predator-prey interaction is aggregated (see \code{\link[=plot_diet_comp]{plot_diet_comp()}} for the
aggregation conventions):
\itemize{
\item prey-age aggregated (predator age resolved): line plot of observed vs
estimated diet proportion against predator age, with 95\% CI ribbons;
\item predator aggregated (prey age resolved): line plot against prey age;
\item both aggregated: dodged bar plot of observed vs estimated proportion;
\item fully disaggregated: predator-age x prey-age bubble grids (observed /
estimated / Pearson residual).
}

The observed proportions, fitted proportions and Pearson residuals come from
the diet data path (\code{quantities$diet_hat} / \code{residuals(source = "diet")} /
\code{data_list$diet_data}) via the internal \code{.diet_plot_data()} helper. Observed
95\% CI ribbons are a normal approximation to the binomial proportion from
\code{Sample_size}. Estimated 95\% CI ribbons are drawn only when the \code{sdreport}
exposes a \code{diet_hat} standard error; the C++ template \code{REPORT()}s but does
not \code{ADREPORT()} \code{diet_hat}, so the estimated ribbon is unavailable unless the
template is changed to \code{ADREPORT(diet_hat)} (the code path is retained so it
activates automatically if that ever happens, or when \code{sdrep} is \code{NULL}).
}
\examples{
\dontrun{
plot_diet_comp2(my_msm_fit)
}
}
\seealso{
\code{\link[=plot_diet_comp]{plot_diet_comp()}}, \code{\link[=plot_diet_comp1]{plot_diet_comp1()}}
}
