% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-ceattle_equations_v11.R
\name{calculate_fishing_mortality}
\alias{calculate_fishing_mortality}
\title{Calculate Fishing Mortality and FSPRs}
\usage{
calculate_fishing_mortality(
  n_flt,
  nspp,
  nages,
  max_nages,
  nsex,
  max_nsex,
  nyrs,
  nyrs_hind,
  flt_spp,
  flt_type,
  sel,
  ln_mean_F,
  F_dev,
  Ftarget,
  Fmult,
  Flimit,
  QnormHCR,
  forecast,
  proj_F_prop,
  HCR
)
}
\arguments{
\item{n_flt}{Integer. The number of fleets.}

\item{nspp}{Integer. The number of species.}

\item{max_nages}{Integer. The number of age classes considered for each species.}

\item{max_nsex}{Integer. The number of sexes considered for each species.}

\item{nyrs}{Integer. The total number of years for the analysis.}

\item{nyrs_hind}{Integer. The number of years for hindcasting.}

\item{flt_spp}{Integer vector. A vector indicating the species associated with each fleet.}

\item{flt_type}{Integer vector. A vector indicating the type of each fleet (1 for active fleets).}

\item{sel}{Array. An array of selectivity values with dimensions (n_flt, max_nsex, max_nages, nyrs).}

\item{ln_mean_F}{Numeric vector. The natural logarithm of mean fishing mortality for each fleet.}

\item{F_dev}{Array. An array of fishing mortality deviations with dimensions (n_flt, nyrs).}

\item{Ftarget}{Numeric vector. The target fishing mortality for each species.}

\item{Fmult}{Numeric vector. Multipliers for fishing mortality for each species.}

\item{Flimit}{Numeric vector. Limit fishing mortality for each species.}

\item{QnormHCR}{Numeric vector. HCR normalization values for each species.}

\item{forecast}{Integer vector. A vector indicating whether a forecast is being run for each species (0 or 1).}

\item{proj_F_prop}{Numeric vector. Projected fishing proportions for each fleet.}

\item{HCR}{}
}
\value{
A list containing:
\item{F_spp}{Array of total fishing mortality by species and year.}
\item{F_flt_age}{Array of fishing mortality by fleet, sex, age, and year.}
\item{F_spp_age}{Array of total fishing mortality by species, sex, age, and year.}
\item{Ftarget_age_spp}{Array of target fishing mortality by species, sex, age, and year.}
\item{Flimit_age_spp}{Array of limit fishing mortality by species, sex, age, and year.}
}
\description{
This function calculates fishing mortality and fishing selectivity for multiple fleets and species over a specified number of years.
}
\examples{
# Example usage of the function
results <- calculate_fishing_mortality(n_flt = 5, nspp = 3, nyrs = 10,
                                         max_nsex = 2, max_nages = 5, nyrs = 20,
                                         nyrs_hind = 10, flt_spp = c(1, 2, 1, 3, 2),
                                         flt_type = c(1, 1, 1, 1, 1),
                                         sel = array(runif(5 * 2 * 5 * 10), dim = c(5, 2, 5, 10)),
                                         ln_mean_F = runif(5),
                                         F_dev = array(runif(5 * 10), dim = c(5, 10)),
                                         Ftarget = runif(3),
                                         Fmult = runif(3),
                                         Flimit = runif(3),
                                         QnormHCR = runif(3),
                                         forecast = c(1, 1, 0),
                                         proj_F_prop = runif(5))

}
