% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-build_linkage.R
\name{build_linkage}
\alias{build_linkage}
\title{Formula-driven linkage specifications for Rceattle processes}
\description{
These helpers let users describe how a process parameter (e.g.
\code{log_alpha} for the Beverton-Holt SRR, \code{log_M} for natural mortality,
\code{log_K} for von Bertalanffy growth) depends on environmental
covariates and on stratifying factors (species, sex). They produce:
}
\details{
\enumerate{
\item an \code{Rceattle_linkage_spec} object that captures the user's
intent (formula + grouping) without committing to a global
column index, and
\item a \code{\link[=materialize_linkage]{materialize_linkage()}} step that, given the env data and
stratum levels, expands the spec into the canonical long-format
linkage-table rows consumed by TMB.
}

Splitting capture from materialization lets \code{fit_mod()} pool specs
from every process, build a single shared design matrix, and assign
globally consistent \code{X_col} indices in one place.
}
\keyword{internal}
