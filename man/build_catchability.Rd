% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-build_srr_and_M.R
\name{build_catchability}
\alias{build_catchability}
\title{Catchability specification}
\usage{
build_catchability(linkages = NULL)
}
\arguments{
\item{linkages}{Optional named list of \code{\link[=linkage_spec]{linkage_spec()}} objects keyed by
catchability parameter. The only parameter is \code{q}. Use \code{by = ~ fleet} for
a separate coefficient per fleet, and the \code{fleet} argument of
\code{\link[=linkage_spec]{linkage_spec()}} to restrict a spec to particular fleets.}
}
\value{
A list of catchability settings for \code{\link[=fit_mod]{fit_mod()}}.
}
\description{
Describes how survey/index catchability is estimated. At present its only
role is to carry environmental linkages on \code{q}, which supersede the
\code{Estimate_q = "Environmental"} / \code{Time_varying_q} column pair: instead of
naming \code{env_data} columns by position, the effect is written as a formula
and can carry priors, bounds and an estimation phase like any other
linkage.
}
\examples{
\donttest{
# One temperature effect on q, estimated separately for each fleet
build_catchability(linkages = list(
  q = linkage_spec(~ temp, by = ~ fleet)))

# Restrict it to fleets 1 and 3, with a prior on the slope
build_catchability(linkages = list(
  q = linkage_spec(~ temp, by = ~ fleet, fleet = c(1, 3),
                   priors = list(temp = prior_normal(0, 1)))))
}

}
