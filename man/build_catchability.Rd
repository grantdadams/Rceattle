% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-build_catchability.R
\name{build_catchability}
\alias{build_catchability}
\title{Catchability specification}
\usage{
build_catchability(linkages = NULL)
}
\arguments{
\item{linkages}{Optional named list of \code{\link[=linkage_spec]{linkage_spec()}} objects keyed by
catchability parameter. The only parameter is \code{q}. Coefficients are per
fleet by default (\code{by = ~ fleet}); use the \code{fleet} argument of
\code{\link[=linkage_spec]{linkage_spec()}} to restrict a spec to particular fleets.}
}
\value{
A list of catchability settings for \code{\link[=fit_mod]{fit_mod()}}.
}
\description{
Carry environmental linkages on survey/index catchability \code{q}. The effect of
an \code{env_data} covariate is written as a formula and can carry priors, bounds,
and an estimation phase like any other linkage.
}
\examples{
\donttest{
# One temperature effect on q per fleet. `by = ~ fleet` is the default, and a
# fleet without an estimated survey catchability is not linkable, so at fit time
# restrict the linkage to the estimated-q fleets (here fleet 7 of BS2017SS).
build_catchability(linkages = list(
  q = linkage_spec(~ temp, fleet = 7)))

# Restrict it to fleets 1 and 3, with a prior on the slope
build_catchability(linkages = list(
  q = linkage_spec(~ temp, fleet = c(1, 3),
                   priors = list(temp = prior_normal(0, 1)))))
}

}
