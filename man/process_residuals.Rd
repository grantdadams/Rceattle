% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/6-process_residuals.R
\name{process_residuals}
\alias{process_residuals}
\title{Process residuals for an Rceattle model's random-effect processes}
\usage{
process_residuals(
  object = NULL,
  process = c("recruitment", "initial", "catchability", "all"),
  seed = 123,
  fit = NULL
)
}
\arguments{
\item{object}{A fitted \code{Rceattle} model. The targeted deviations must be estimated -- as random effects (e.g. \code{random_rec = TRUE}) or as penalized fixed effects -- with a usable covariance.}

\item{process}{One of \code{"recruitment"}, \code{"initial"}, \code{"catchability"}, or
\code{"all"}.}

\item{seed}{Seed for the posterior draw. Default 123.}

\item{fit}{deprecated name for \code{object}, still accepted so existing
scripts keep working. Supplying both is an error.}
}
\value{
A data frame of class \code{rceattle_osa} (so it can be passed to
\code{\link[=osa_diagnostics]{osa_diagnostics()}} and \code{\link[=plot.rceattle_osa]{plot.rceattle_osa()}}) with one row per deviation:
columns \code{source} (the process), \code{fleet}, \code{species}, \code{year},
\code{age_length_bin}, and \code{residual}.
}
\description{
One-sample process residuals for the model's random-effect (or penalized)
process deviations, in the style of SAM's \code{procres()} (Nielsen and Berg
2014). They validate the \emph{process} model -- whether the deviations behave like
their assumed iid normal process -- a complement to the observation-based
\code{\link[=osa_residuals]{osa_residuals()}}.

Each set of deviations carries a Gaussian process prior in the model. The
posterior \emph{mode} of the deviations is shrunk toward that prior, so -- following
SAM -- a single draw is taken from the joint posterior of the deviations (from
the joint precision when they are random effects, or the fixed-effect
covariance when they are penalized fixed effects) and standardized by the
process standard deviation. Under a correctly specified process these are
approximately iid N(0, 1). Because a random draw is used the residuals are
stochastic; set \code{seed} for reproducibility.

Supported processes and their deviations:
\describe{
\item{\code{"recruitment"}}{\code{rec_dev}, prior \code{N(-bias_adjust_proc * R_sd^2/2, R_sd^2)} per species.}
\item{\code{"initial"}}{\code{init_dev}, prior \code{N(-bias_adjust_proc * R_sd^2/2, R_sd^2)} per species.}
\item{\code{"catchability"}}{\code{index_q_dev}, prior \code{N(0, q_dev_sd^2)} per index.}
}
\code{"all"} returns every supported process present in the fit. Selectivity and
natural-mortality deviations (which use random-walk / 2D-AR1 priors) are not
yet supported. Recruitment and initial-abundance residuals are exact (their
priors are iid). Catchability residuals are exact only for the iid deviate
prior (\code{Time_varying_q = 1} or \code{2}); for a random-walk or AR1 catchability
prior the marginal-SD standardization ignores the prior correlation, so those
residuals are approximate and a warning is emitted.
}
\section{On a DSEM fit}{

A DSEM makes the recruitment deviations the latent states of a Gaussian
Markov random field, so there is no per-year normal prior to divide by.
\code{"recruitment"} instead whitens the states with the process precision:
conditioning on the states the model was given -- the observed covariates --
the estimated states have precision \eqn{Q_{EE}}, and
\eqn{r = U (x_E - m)} with \eqn{Q_{EE} = U^{\top} U} has covariance
\eqn{I}. So the claim is the same as on the standard path, and exact in the
same sense. One residual is returned per hindcast year per species; a
covariate's own latent states are not residualized here.

\code{"initial"} standardizes by the recruitment SD the density actually used,
which under a DSEM comes from the SEM's variance path rather than from
\code{R_log_sd} (mapped out, and a placeholder). \code{"catchability"} is untouched by
a DSEM.
}

\references{
Nielsen, A., and Berg, C.W. 2014. Estimation of time-varying selectivity in
stock assessments using state-space models. Fish. Res. 158:96-101.
}
\seealso{
\code{\link[=osa_residuals]{osa_residuals()}}, \code{\link[=osa_diagnostics]{osa_diagnostics()}}
}
