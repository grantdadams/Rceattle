% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1-data_requirements.R
\name{data_requirements}
\alias{data_requirements}
\title{Report which data inputs a model configuration requires}
\usage{
data_requirements(
  data_list = NULL,
  msmMode = 0,
  growth_model = 0,
  estDynamics = 0,
  selectivity = NULL,
  index_loglike = NULL,
  Ceq = NULL
)
}
\arguments{
\item{data_list}{Optional. An existing Rceattle data list (e.g. from
\code{\link[=read_data]{read_data()}} / \code{\link[=build_data]{build_data()}}, or a bundled dataset). When supplied, the
configuration is read from it and the convenience arguments are ignored.}

\item{msmMode}{Predation mode used to build the configuration when \code{data_list}
is \code{NULL}. \code{0} single-species (default), \verb{> 0} multispecies.}

\item{growth_model}{Growth mode (scalar or per-species). \verb{> 0} estimates
growth and requires \code{caal_data}.}

\item{estDynamics}{Numbers-at-age mode (scalar or per-species). \verb{> 0} fixes
numbers-at-age and requires \code{NByageFixed}.}

\item{selectivity}{Optional character vector of per-fleet selectivity forms
(used only when \code{data_list} is \code{NULL}) so the \code{emp_sel} requirement
(\code{Selectivity = "Fixed"}) can be evaluated.}

\item{index_loglike}{Optional character vector of per-fleet index likelihoods
(used only when \code{data_list} is \code{NULL}) so the \code{index_cov} requirement
(\code{"MVN"}) can be evaluated.}

\item{Ceq}{Optional per-species consumption-equation codes (used only when
\code{data_list} is \code{NULL}); \verb{> 1} requires an environmental temperature index.}
}
\value{
A \code{data.frame} with one row per data element and columns:
\describe{
\item{\code{element}}{the \code{data_list} element name.}
\item{\code{category}}{grouping (dimensions / biology / fishery / composition /
predation / environment).}
\item{\code{status}}{\code{"Required"}, \code{"Optional"}, or \code{"Ignored"}.}
\item{\code{condition}}{the condition under which the element is required
(\code{"always"} for the core backbone).}
\item{\code{default}}{for Optional elements, the \code{\link[=clean_data]{clean_data()}} default used
when the element is absent.}
}
Rows are ordered Required, then Optional, then Ignored.
}
\description{
Introspects the Rceattle data-element catalogue and reports, for a given model
configuration, which top-level \code{data_list} elements are \strong{Required},
\strong{Optional} (used if supplied, otherwise default-filled by \code{\link[=clean_data]{clean_data()}}), or
\strong{Ignored} (not consulted because the feature that would use them is switched
off). It answers "what do I actually need to supply for \emph{this} model?" without
having to read \code{\link[=data_check]{data_check()}} or the switch tables -- the same conditions
\code{\link[=data_check]{data_check()}} enforces at fit time (they share one declarative table).
}
\details{
The configuration can be given either as an existing (possibly partial)
\code{data_list} -- its switches are normalised through \code{\link[=clean_data]{clean_data()}} /
\code{\link[=switch_check]{switch_check()}} so the conditions evaluate against filled defaults -- or,
when no \code{data_list} is supplied, built from the convenience arguments.

Requirements are \emph{conditional}: e.g. \code{diet_data}, \code{ration_data} and the
bioenergetics scalars are Ignored under single-species (\code{msmMode = 0}) but
Required under multispecies (\code{msmMode > 0}); \code{caal_data} is Optional unless
\code{growth_model > 0}; \code{NByageFixed} is Ignored unless \code{estDynamics > 0};
\code{emp_sel} is Required only for fleets with \code{Selectivity = "Fixed"};
\code{index_cov} only for \code{Index_loglike = "MVN"}.
}
\examples{
# Single-species: predation/diet inputs are Ignored, comp_data Optional.
data_requirements(msmMode = 0)

# Multispecies: diet_data and bioenergetics become Required.
data_requirements(msmMode = 1)

# From a bundled dataset.
data_requirements(BS2017SS)
}
\seealso{
\code{\link[=data_check]{data_check()}}, \code{\link[=clean_data]{clean_data()}}, \code{\link[=build_data]{build_data()}}.
}
