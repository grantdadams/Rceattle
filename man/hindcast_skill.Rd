% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/9-hindcast_skill.R
\name{hindcast_skill}
\alias{hindcast_skill}
\title{Forecast skill across retrospective peels}
\usage{
hindcast_skill(
  object = NULL,
  peels = 5,
  quantity = c("ssb", "biomass", "R"),
  reference = c("model", "observed"),
  forecast_rec = c("model", "mean"),
  retro = NULL,
  ...
)
}
\arguments{
\item{object}{A fitted Rceattle model (the full time series).}

\item{peels}{Number of peels. Passed to \code{\link[=retrospective]{retrospective()}}.}

\item{quantity}{Quantities to score against the full model. Any of \code{"ssb"},
\code{"biomass"}, \code{"R"}. Ignored when \code{reference = "observed"}.}

\item{reference}{What to score the projection against.
\code{"model"} (default) compares each peel's projection to the full time-series
model's estimate, taking that as the best available account of what
happened. \code{"observed"} is classic hindcast cross-validation: it compares the
peel's PREDICTED SURVEY INDEX to the index values actually observed in the
held-out years. Both can be asked for. \code{"model"} scores the quantity a
projection is used for (SSB, recruitment); \code{"observed"} scores the only
thing that was really measured, and is the version comparable to MASE
values published for other assessment platforms.}

\item{forecast_rec}{How the peeled years get their recruitment.
\code{"model"} (default here) uses the model's own projection rule, in
precedence order: \code{proj_mean_rec = TRUE} projects at mean recruitment,
whatever process the model carries, because that is what the setting means;
otherwise the LATENT STATES supply it where the deviations are random
effects (\code{random_rec = TRUE}, or a DSEM), so an AR1's autocorrelation or a
DSEM's lagged and covariate paths propagate into the forecast; otherwise
recruitment comes off the stock-recruit curve. \code{"mean"} forces the
historical mean for all of them, which is \code{\link[=retrospective]{retrospective()}}'s default and
the convention Mohn's rho is computed under.

The default differs from \code{\link[=retrospective]{retrospective()}}'s on purpose. A peel's forecast
years are hindcast years, so \code{proj_mean_rec} cannot reach them by itself,
and with \code{"mean"} every model forecasts identically -- which makes this
function unable to tell projection methods apart, the one thing it exists
to do. Measured on the GOA arrowtooth model, \code{proj_mean_rec = TRUE} and
\code{FALSE} returned byte-identical MASE under \code{"mean"}.}

\item{retro}{Optionally an already-computed \code{\link[=retrospective]{retrospective()}} result, to
avoid refitting when both are wanted. Note a \code{retro} computed with
\code{\link[=retrospective]{retrospective()}}'s defaults carries \code{forecast_rec = "mean"}.}

\item{...}{Passed to \code{\link[=retrospective]{retrospective()}} (\code{cores}, \code{getsd}, \code{rescale}).}
}
\value{
A list with
\describe{
\item{\code{mase}}{one row per peel x species x quantity, with \code{mase},
\code{mae_forecast}, \code{mae_naive} and the number of forecast years scored.}
\item{\code{by_year}}{the underlying series: peel, species, quantity, year,
years-ahead, \code{forecast}, \code{reference}, \code{naive}.}
}
}
\description{
Scores how well each retrospective peel's projection recovers the full
time-series model's estimate over the years the peel did not see, given the
known catch. The full model is taken as the best available estimate of what
happened, so this measures forecast skill rather than the estimation
consistency \code{\link[=retrospective]{retrospective()}} reports through Mohn's rho.
}
\details{
Each peel drops the last \code{i} years of data, refits, and then solves fishing
mortality against the catch that was actually taken in those years -- so the
projection differs from the full model only in what it could not know:
the recruitment, and anything downstream of it. That makes this the natural
comparison for recruitment-projection assumptions, e.g.
\code{proj_mean_rec = TRUE} against \code{FALSE} against a DSEM.

Skill is reported as the mean absolute scaled error (MASE), following the
hindcast cross-validation of Kell et al. (2016) but scored against the full
model's estimate rather than against held-out observations:

\deqn{MASE = \frac{\overline{|forecast - reference|}}{\overline{|naive - reference|}}}

The naive forecast is the peel's own terminal estimate carried forward,
i.e. "assume nothing changes". So:
\itemize{
\item \code{MASE < 1} -- the projection beats assuming no change.
\item \code{MASE > 1} -- it is worse than assuming no change, which usually means
the recruitment assumption is pulling the projection the wrong way.
\item \code{MASE = 1} -- no better than persistence.
}

A MASE is undefined when the naive forecast happens to be exactly right
(a zero denominator); those rows come back \code{NA} rather than \code{Inf}.

Read short horizons with care. At \code{years_ahead = 1} the denominator is a
single \eqn{|naive - reference|}, so a peel whose persistence forecast lands
close to the reference by chance produces a very large MASE that says more
about the denominator than about the projection -- values in the tens appear
routinely at one-year horizons and settle by three. Compare the deeper peels,
or the median across peels, rather than any single number. This is a property
of the scaling, not of the model, and it is shared with the \code{ss3diags}
implementation the definition matches.
}
\references{
Kell, L.T., Kimoto, A., Kitakado, T. (2016) Evaluation of the prediction
skill of stock assessment using hindcasting. \emph{Fisheries Research} 183,
119-127.
}
