% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7-plot_data.R
\name{plot_data}
\alias{plot_data}
\title{Timeline of data used in the model likelihoods}
\usage{
plot_data(
  Rceattle,
  file = NULL,
  subplots = 1:2,
  datatypes = "all",
  fleets = "all",
  species = "all",
  ghost = FALSE,
  fleetcol = "default",
  width = 7,
  height = 5,
  res = 200,
  ptsize = 10,
  margins = c(5.1, 2.1, 2.1, 10.1),
  cex = 2,
  lwd = 12,
  maxsize = 1,
  alphasize = 1,
  mainTitle = FALSE,
  cex.main = 1
)
}
\arguments{
\item{Rceattle}{Either a single Rceattle model object exported from
\code{Rceattle::fit_mod()}, or a raw \code{data_list} (e.g. one of the
bundled datasets such as \code{BS2017MS}).}

\item{file}{Path/prefix used to save the figure. If \code{NULL} (default) the
plot is drawn to the active device only. When supplied, the figure is
written to \code{paste0(file, "_data_plot.png")} (and \verb{_data_plot2.png} for
the bubble subplot).}

\item{subplots}{Integer vector controlling which subplots are produced:
\itemize{
\item 1 — equal-size points showing presence/absence by year/fleet
\item 2 — points scaled to relative quantity / precision within each
data type (catch tonnage, 1/SE for indices, sample size for comps)
}}

\item{datatypes}{Either \code{"all"} or a subset of
\code{c("catch", "index", "agecomp", "lencomp", "caal", "diet")}.}

\item{fleets}{Either \code{"all"} or an integer vector of \code{Fleet_code}s to
include. Diet "fleets" are encoded after the regular fleets (one per
predator species).}

\item{species}{Either \code{"all"} or an integer vector of species indices to
include.}

\item{ghost}{Logical. If \code{TRUE}, also include rows excluded from the
likelihood (\code{Year < 0}). Defaults to \code{FALSE}.}

\item{fleetcol}{Either \code{"default"} or a vector of colors (one per fleet
shown after subsetting). Diet entries get an additional color appended.}

\item{width, height}{Figure dimensions in inches.}

\item{res}{Resolution (dpi) for the saved PNG.}

\item{ptsize}{Pointsize passed to \code{png()}.}

\item{margins}{\code{par("mar")} for the plot. Increase the right margin if
long fleet names are clipped.}

\item{cex}{Character expansion for points showing isolated years.}

\item{lwd}{Line width for runs of consecutive years (subplot 1).}

\item{maxsize}{Max bubble radius (in plot units) for subplot 2.}

\item{alphasize}{Bubble fill transparency (0–1).}

\item{mainTitle}{Logical; if \code{TRUE} add a default title.}

\item{cex.main}{Title character expansion.}
}
\value{
Invisibly, a list with \code{typetable} — the long data frame underlying
the plot (year, fleet, data type, relative size).
}
\description{
Plots a timeline of presence/absence (and optionally relative quantity) of
the data sources contributing to an Rceattle model's likelihood, by year and
fleet. Modelled after Stock Synthesis's \code{r4ss::SSplotData()}.
}
\details{
Years with \code{Year > 0} contribute to the likelihood; rows with \code{Year < 0}
(kept for fitting comparison only) are shown when \code{ghost = TRUE}. Diet data
uses \code{Year == 0} for the fixed reference stomach composition; those entries
are plotted at the model \code{endyr} and labelled per predator species.
}
