% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7-plot_helpers.R
\name{rceattle-plot-args}
\alias{rceattle-plot-args}
\title{Shared plotting arguments}
\arguments{
\item{Rceattle}{A single \code{\link[=fit_mod]{fit_mod()}} object or a list of them (overlaid).}

\item{file}{Optional file stem; the figure is written to
\verb{<file>_<suffix>.png} if given.}

\item{model_names}{Legend labels for the models.}

\item{species}{Species to include, as indices (\code{c(1, 3)}), names, a logical
mask, or \code{"all"}. Default \code{NULL} plots every species. Species \strong{labels}
belong in \code{spnames}; a character \code{species} that matches no species name is
read as labels for back-compatibility.}

\item{spnames}{Species labels, length \code{nspp}. Default: the model's own.}

\item{line_col}{Line colours; names, hex, or base-graphics integers. \code{NULL}
uses the colorblind-safe Okabe-Ito palette. See above for what it maps to.}

\item{lwd}{Line width on the base-graphics scale: the default \code{3} renders as
a standard-weight ggplot line. A vector varies it across series.}

\item{lty}{Line type. A vector varies it across series.}

\item{alpha}{Transparency of confidence ribbons and shaded areas.}

\item{add_ci}{Add a 95\% confidence interval. Only available where the
plotted quantity carries standard errors; warns and draws none otherwise.}

\item{minyr, maxyr}{First / last year to plot.}

\item{incl_proj}{Include the projection years, with a dashed divider at the
last hindcast year.}

\item{incl_mean}{Add a horizontal line at the hindcast mean of each series.}

\item{width, height}{Saved figure size in inches.}

\item{right_adj, top_adj, mod_cex, legend.pos, single.plots, save, theta}{Base-graphics arguments from before the ggplot2 rewrite. Accepted so
existing scripts keep running, and ignored. The returned object is a
ggplot, so margins, legend placement and text size are set on it directly
(e.g. \code{p + ggplot2::theme(legend.position = "right")}).}
}
\value{
A \code{ggplot} object.
}
\description{
The \verb{plot_*()} functions take a common vocabulary, documented here once and
inherited by each of them.
}
\section{How \code{line_col} and \code{lty} are applied}{


They supply values for whichever \strong{discrete variable the plot already
encodes with that aesthetic}, matched in level order -- which is not always
the model. In \code{\link[=plot_b_eaten_prop]{plot_b_eaten_prop()}} colour separates predators; in
\code{\link[=plot_ration]{plot_ration()}} line type separates sexes. Check what the figure's legend
names before assuming a colour vector maps to your models.

Where colour encodes a continuous variable -- the year fan in
\code{\link[=plot_selectivity]{plot_selectivity()}} and \code{\link[=plot_mortality]{plot_mortality()}} -- \code{line_col} supplies the ramp
anchors instead: one colour draws the fan in that colour, several interpolate
between them.
}

\keyword{internal}
