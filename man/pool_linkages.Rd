% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-build_linkage.R
\name{pool_linkages}
\alias{pool_linkages}
\title{Pool a collection of linkage specs into a global table + design matrix}
\usage{
pool_linkages(spec_groups, env_data, strata = list())
}
\arguments{
\item{spec_groups}{named list whose names are process labels
(one of \link{LINKAGE_PROCESSES}) and whose elements are themselves
named lists of \code{\link[=linkage_spec]{linkage_spec()}} objects keyed by parameter name.
Empty entries are skipped.}

\item{env_data}{data.frame of environmental covariates (one row per
model year, including any factor columns referenced by spec
formulas).}

\item{strata}{named list of stratum-level integer vectors, e.g.
\code{list(species = 1:nspp, sex = 1:nsex)}.}
}
\value{
A list with components:
\describe{
\item{table}{An \code{Rceattle_linkage_table} with \code{X_col} indices
remapped to the global design matrix. May have zero rows.}
\item{X}{A numeric matrix \verb{[nrow(env_data) x n_global_cols]} with
\code{colnames(X)} = the union of design-column names across specs.}
\item{design_names}{\code{colnames(X)} (separate copy for caller
convenience).}
}
}
\description{
Collects every spec that's been registered against a process,
materializes each against the supplied \code{env_data} and \code{strata},
unions their design-matrix columns by name into a single shared
design matrix \code{X}, and remaps each row's local \code{X_col} index to the
global column. Used inside \code{fit_mod()} once all \verb{build_*()} outputs
have populated \code{data_list}.
}
\details{
Two design columns from different specs that share a name are
assumed to carry identical numeric values (which holds for
deterministic terms generated by \code{\link[stats:model.matrix]{stats::model.matrix()}} over the
same \code{env_data}). The first occurrence wins on order; subsequent
specs reuse the column.
}
\keyword{internal}
