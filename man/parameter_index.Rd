% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-parameter_index.R
\name{parameter_index}
\alias{parameter_index}
\title{Locate every estimated parameter in the model's own coordinates}
\usage{
parameter_index(object)
}
\arguments{
\item{object}{A fitted Rceattle model from \code{\link[=fit_mod]{fit_mod()}}.}
}
\value{
A data frame with one row per estimated parameter and columns
\code{par_index} (position in \code{obj$par}), \code{block}, \code{species}, \code{fleet}, \code{sex},
\code{age}, \code{bin}, \code{year}, \code{slot}, \code{n_cells} and \code{label}.
}
\description{
One row per estimated (fixed-effect) parameter, giving the fleet, species,
sex, age, bin, year or slot it occupies.
}
\details{
TMB names each element of \code{obj$par} after its parameter block, so a
convergence diagnostic can report that \code{sel_coff_dev} is non-identifiable but
not which of its elements.

The mapping is recovered by pushing a tagged parameter vector back through
TMB's own \code{parList()}, so it reflects \code{build_map()} exactly: parameters
mapped off are absent, and fleets sharing a \code{Selectivity_index} or
\code{Catchability_index} appear once, as the single parameter they are, with
\code{n_cells} recording how many array cells they drive.

Coordinate columns are \code{NA} where the axis does not apply to a block. The
linkage and environmental-covariate blocks (\code{beta_linkage}, \code{M1_beta}, ...)
are indexed by a linkage-table row rather than a model coordinate, so they
carry no coordinates and are reported by element number.

A selectivity \code{slot} is named from the fleet's \code{Selectivity}: slot 2 of
\code{sel_inf} is a descending inflection for the double-logistic family, the
right-tail floor under \code{DoubleNormal}, and the age-1 selectivity under
\code{LogisticPM}.
}
\seealso{
\code{\link[=parameter_dictionary]{parameter_dictionary()}} for what each block means.
}
