% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-rceattle_class.R
\name{simulate.Rceattle}
\alias{simulate.Rceattle}
\title{Simulate data from a fitted Rceattle model}
\usage{
\method{simulate}{Rceattle}(object, nsim = 1, seed = NULL, process = FALSE, ...)
}
\arguments{
\item{object}{An object of class \code{"Rceattle"} returned by \code{\link[=fit_mod]{fit_mod()}}.}

\item{nsim}{Number of replicate data sets to draw. Default 1.}

\item{seed}{Optional seed. Set as \code{set.seed(seed)} and the previous
random-number state restored afterwards, so simulating does not disturb a
stream the caller is using. Recorded on the result as \code{attr(, "seed")}.}

\item{process}{Which process error to redraw alongside the observations.
\code{FALSE} (default) keeps the fitted deviations, so replicates differ only in
observation error. See \code{\link[=sim_mod]{sim_mod()}} for the alternatives.}

\item{...}{Currently unused.}
}
\value{
A list of \code{nsim} \code{data_list} objects -- always a list, including at
\code{nsim = 1}, so callers do not have to special-case the length. Each element
carries the drawn process deviations as \code{attr(, "process_sim")} when
\code{process} redrew something.
}
\description{
The \code{\link[stats:simulate]{stats::simulate()}} method for CEATTLE fits: draws \code{nsim}
replicate data sets from the fitted observation model, and optionally from
the process model as well.
}
\details{
A thin wrapper on \code{\link[=sim_mod]{sim_mod()}}, which does the work and documents the
observation model in full. Use whichever reads better; \code{sim_mod()} also
offers \code{simulate = FALSE} for expected values rather than draws, which is
not simulation and so has no place in this method.

Every draw is taken by the TMB model itself, beside the likelihood that
defines it, so the simulated data and the density that will be fitted to them
cannot drift apart. That needs a live \verb{$obj}, which a model loaded from disk
has and a \code{\link[=model_average]{model_average()}} result does not -- see \code{\link[=sim_mod]{sim_mod()}}.
}
\examples{
\dontrun{
fit  <- fit_mod(data_list = BS2017SS, estimateMode = 1)
reps <- simulate(fit, nsim = 10, seed = 1)
# ...and with recruitment redrawn as well:
reps <- simulate(fit, nsim = 10, seed = 1, process = "recruitment")
truth <- attr(reps[[1]], "process_sim")$rec_dev
}
}
\seealso{
\code{\link[=sim_mod]{sim_mod()}} for the observation model and the \code{process} options,
\code{\link[=self_test]{self_test()}} for simulating and refitting in one step.
}
