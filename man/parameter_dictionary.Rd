% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-parameter_dictionary.R
\name{parameter_dictionary}
\alias{parameter_dictionary}
\title{Look up what a CEATTLE parameter is}
\usage{
parameter_dictionary(internal = NULL, process = NULL)
}
\arguments{
\item{internal}{Character vector of internal parameter names, as they appear
in \code{names(fitted$estimated_params)} or in an \code{sdreport()}. Default \code{NULL}
returns every parameter.}

\item{process}{Character vector restricting the table to one or more parts of
the model: \code{"recruitment"}, \code{"mortality"}, \code{"growth"}, \code{"fishing"},
\code{"catchability"}, \code{"selectivity"}, \code{"observation"}, \code{"predation"},
\code{"linkage"} or \code{"internal"}.}
}
\value{
A data frame with one row per parameter and columns \code{internal},
\code{natural}, \code{process}, \code{meaning} and \code{dims}.
}
\description{
The TMB parameter vector uses transformed, abbreviated names --
\code{log_M1}, \code{R_log_sd}, \code{index_log_q}. This returns the table mapping each one
to the quantity it represents on its natural scale, which process it belongs
to, what it means, and its dimensions.
}
\details{
A name beginning \code{log_}, or ending \verb{_log_sd}, is estimated on the log scale,
so \code{exp()} turns it into the quantity named in the \code{natural} column. Most
fitted values are already back-transformed in \code{fitted$quantities}, so this is
mainly needed when reading \code{estimated_params} or an \code{sdreport()} directly.

Dimensions use the model's own notation: \code{nspp} species, \code{nsex} sexes,
\code{nages} age bins, \code{nyrs} years including the projection, \code{nyrs_hind} hindcast
years only, \code{n_flt} fleets and \code{n_sel} selectivity blocks. \code{n_sel} equals
\code{n_flt}: fleets sharing a \code{Selectivity_index} share one block through the
map, not through a narrower array.
}
\examples{
# The whole table
head(parameter_dictionary())

# What is R_log_sd?
parameter_dictionary("R_log_sd")

# Everything governing selectivity
parameter_dictionary(process = "selectivity")

# Search the meanings by keyword
dict <- parameter_dictionary()
dict[grep("catchability", dict$meaning, ignore.case = TRUE), ]
}
\seealso{
\code{\link[=fit_mod]{fit_mod()}} for the fitted object these names come from.
}
