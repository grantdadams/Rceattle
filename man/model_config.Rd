% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-model_config.R
\name{model_config}
\alias{model_config}
\title{Build a model-configuration slot for a data list}
\usage{
model_config(
  msmMode = 0,
  initMode = "NonEquilibrium",
  avgnMode = 0,
  suitMode = 0,
  niter = 3,
  HCR = build_hcr(),
  recFun = build_srr(),
  M1Fun = build_M1(),
  growthFun = build_growth(),
  qFun = build_catchability(),
  selFun = build_selectivity(),
  compFun = build_composition()
)
}
\arguments{
\item{msmMode}{Predation-mortality mode (see \code{\link[=fit_mod]{fit_mod()}}). Default \code{0}.}

\item{initMode}{Population initialization (see \code{\link[=fit_mod]{fit_mod()}}). Default
\code{"NonEquilibrium"}.}

\item{avgnMode}{Average-N mode. Default \code{0}.}

\item{suitMode}{Suitability mode. Default \code{0}.}

\item{niter}{Number of predation iterations. Default \code{3}.}

\item{HCR}{A harvest-control-rule specification from \code{\link[=build_hcr]{build_hcr()}}.}

\item{recFun}{A stock-recruit specification from \code{\link[=build_srr]{build_srr()}}.}

\item{M1Fun}{A natural-mortality specification from \code{\link[=build_M1]{build_M1()}}.}

\item{growthFun}{A growth specification from \code{\link[=build_growth]{build_growth()}}.}

\item{qFun}{A catchability specification from \code{\link[=build_catchability]{build_catchability()}}.}

\item{selFun}{A selectivity specification from \code{\link[=build_selectivity]{build_selectivity()}}.}

\item{compFun}{A composition specification from \code{\link[=build_composition]{build_composition()}}.}
}
\value{
An object of class \code{"Rceattle_model_config"} (a named list) to be
stored as \code{data_list$model_config}.
}
\description{
Bundles the model-structure arguments of \code{\link[=fit_mod]{fit_mod()}} -- predation mode,
initialization, harvest control rule, and the \verb{build_*()} process
specifications -- into a single validated object that can be stored on a data
list (\code{data_list$model_config}, e.g. via
\code{build_data(..., model_config = model_config(...))}). A configuration then
travels with the data instead of living only in the \code{\link[=fit_mod]{fit_mod()}} call.
}
\details{
The defaults are exactly \code{\link[=fit_mod]{fit_mod()}}'s own argument defaults, so a data list
carrying \code{model_config()} fits identically to one with no slot at all. When a
data list has a \code{model_config}, \code{\link[=fit_mod]{fit_mod()}} reads each field only for
arguments the caller did \strong{not} pass explicitly; an argument passed to
\code{\link[=fit_mod]{fit_mod()}} always overrides the stored value (matching how \code{\link[=fit_mod]{fit_mod()}}
already treats its arguments). Note that because production scripts often pass
these arguments explicitly at their defaults, such a call still overrides the
slot -- omit the argument to let the configuration take effect.
}
\section{Persistence}{

The configuration is code-side model structure, not one of the workbook data
sheets, so it is \strong{not} written by \code{\link[=write_data]{write_data()}} and does not survive an
xlsx round-trip -- \code{build_data(base = x, model_config = cfg)} piped through
\code{\link[=write_data]{write_data()}} then \code{\link[=read_data]{read_data()}} returns without the slot. Re-attach it in
code, store it alongside the data, or persist it as a documented, git-diffable
YAML with \code{\link[=save_config]{save_config()}} / \code{\link[=load_config]{load_config()}} and apply it with
\code{fit_mod(data_list, config = load_config("run.yaml"))}.
}

\examples{
cfg <- model_config(msmMode = 1, initMode = "NonEquilibrium")
dat <- build_data(base = BS2017MS, model_config = cfg)
# fit_mod(dat) would then fit as multispecies without passing msmMode.
}
\seealso{
\code{\link[=fit_mod]{fit_mod()}}, \code{\link[=build_data]{build_data()}}, \code{\link[=save_config]{save_config()}}, \code{\link[=load_config]{load_config()}}.
}
