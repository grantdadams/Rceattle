% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-save_config.R
\name{save_config}
\alias{save_config}
\title{Save a model run configuration to a documented YAML file}
\usage{
save_config(x, file = "Rceattle_config.yaml", ...)
}
\arguments{
\item{x}{A fitted Rceattle object, a data list carrying \verb{$model_config}, an
\code{Rceattle_run_config}, or an \code{Rceattle_model_config}.}

\item{file}{Output path for the \code{.yaml} file.}

\item{...}{Estimation controls / \code{fit_control} to record (passed to
\code{\link[=run_config]{run_config()}}).}
}
\value{
Invisibly, the \code{Rceattle_run_config} that was written.
}
\description{
Round-trips a full run configuration -- the \code{\link[=model_config]{model_config()}} structure plus
the estimation controls and \code{\link[=fit_control]{fit_control()}} bundle -- to a
git-diffable YAML file, with each field's documentation emitted as a comment
and a spec-tree + provenance header. Only fields that differ from their
defaults are written, so two configurations diff to just their real
differences. The parameter values (\code{inits}/\code{map}/\code{bounds}) are NOT stored;
pair the config with a saved fit for those.
}
\examples{
cfg <- model_config(msmMode = 1, initMode = "FishedEquilibrium")
f <- file.path(tempdir(), "run.yaml")
save_config(cfg, f)
identical(load_config(f)$model_config$msmMode, 1)
}
\seealso{
\code{\link[=load_config]{load_config()}}, \code{\link[=run_config]{run_config()}}, \code{\link[=model_config]{model_config()}}, \code{\link[=fit_mod]{fit_mod()}}.
}
