% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/6-osa_residuals.R
\name{osa_diagnostics}
\alias{osa_diagnostics}
\title{Statistical diagnostics for OSA residuals}
\usage{
osa_diagnostics(osa, nsim = 10000, probs = c(0.025, 0.975), seed = 123)
}
\arguments{
\item{osa}{An \code{rceattle_osa} object from \code{\link[=osa_residuals]{osa_residuals()}}, or a data frame
with \code{residual} and (optionally) \code{type}/\code{fleet} columns.}

\item{nsim}{Number of simulations for the tail-statistic null intervals.
Default 10000.}

\item{probs}{Lower/upper tail probabilities. Default \code{c(0.025, 0.975)}.}

\item{seed}{Seed for the tail-interval simulation (reproducibility).
Default 123.}
}
\value{
A data frame with one row per data source plus an \code{"all"} row, with
columns: \code{source}, \code{type}, \code{fleet}, \code{n}, \code{sdnr}, \code{sdnr_lo}, \code{sdnr_hi},
\code{lower}, \code{lower_lo}, \code{lower_hi}, \code{upper}, \code{upper_lo}, \code{upper_hi}, and the
logical flags \code{sdnr_ok}, \code{lower_ok}, \code{upper_ok} (TRUE when the statistic is
inside its null interval).
}
\description{
Computes the Stewart and Monnahan (2025) statistical diagnostics for a set of
OSA residuals: the standard deviation of the normalized residuals (SDNR) and
the lower/upper tail statistics, each with the 95\% interval expected under
the standard normal null hypothesis (so departures can be judged objectively
rather than by eye). Computed per data source (type x fleet) and overall.

Under a correctly specified model OSA residuals are already iid standard
normal, so the SDNR is simply their sample standard deviation. Its null
interval follows the chi-square result for the sample standard deviation of
\code{n} standard normals (Francis 2014); the tail-statistic null intervals are
obtained by simulation.
}
\references{
Francis, R.I.C.C. 2014. Replacing the multinomial in stock assessment models:
a first step. Fish. Res. 151:70-84.

Stewart, I.J., and Monnahan, C.C. 2025. Can. J. Fish. Aquat. Sci. 82:1-13.
}
\seealso{
\code{\link[=osa_residuals]{osa_residuals()}}
}
