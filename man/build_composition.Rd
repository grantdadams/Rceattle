% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-build_srr_and_M.R
\name{build_composition}
\alias{build_composition}
\title{Composition-weighting specification}
\usage{
build_composition(linkages = NULL)
}
\arguments{
\item{linkages}{Optional named list of \code{\link[=linkage_spec]{linkage_spec()}} objects keyed by
\code{theta_comp} / \code{theta_caal} (per fleet by default, \code{by = ~ fleet}) or
\code{theta_diet} (per predator by default, \code{by = ~ species}).}
}
\value{
A list of composition-weighting settings for \code{\link[=fit_mod]{fit_mod()}}.
}
\description{
Carries \strong{priors} on the Dirichlet-multinomial composition-weighting
overdispersion. The DM weight (the "theta" that scales the effective sample
size) is otherwise an unpenalized free parameter; a linkage lets you put a
prior on it through the same grammar as every other parameter. The three
parameters target the three DM likelihoods:
\describe{
\item{\code{theta_comp}}{age / length composition weight (\code{comp_weights}), per
fleet.}
\item{\code{theta_caal}}{conditional-age-at-length weight (\code{caal_weights}), per
fleet.}
\item{\code{theta_diet}}{diet composition weight (\code{diet_comp_weights}), per
predator.}
}

This is a \strong{prior-only} process: the DM weight is a scalar, not a
year-varying quantity, so only intercept formulas (\code{~ 1}) with a \code{priors}
entry are meaningful. The prior is placed on the natural-scale DM weight
\code{theta = exp(weight)}, so a \code{gamma()} prior reads naturally. A linkage on a
fleet whose \code{Comp_distribution} (or \code{CAAL_distribution}) is not
\code{"DirichletMultinomial"} errors, since the weight has no effect there.
}
\examples{
\donttest{
# A weak gamma prior on the DM overdispersion of every fleet's age comps
build_composition(linkages = list(
  theta_comp = linkage_spec(~ 1, by = ~ fleet,
                            priors = list(`(Intercept)` = gamma(2, 0.5)))))
}

}
