% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-quantity_dictionary.R
\name{quantity_dictionary}
\alias{quantity_dictionary}
\title{Look up what a CEATTLE reported quantity is}
\usage{
quantity_dictionary(quantity = NULL, process = NULL)
}
\arguments{
\item{quantity}{Report names as they appear in \code{names(fit$quantities)},
default \code{NULL} for every documented quantity.}

\item{process}{Character vector restricting the table to one or more parts of
the model: \code{"population"}, \code{"recruitment"}, \code{"mortality"}, \code{"fishing"},
\code{"reference_points"}, \code{"catchability"}, \code{"selectivity"}, \code{"growth"},
\code{"composition"}, \code{"predation"}, \code{"likelihood"}, \code{"linkage"} or
\code{"internal"}.}
}
\value{
A data frame with one row per quantity and columns \code{quantity},
\code{process}, \code{meaning}, \code{units}, \code{dims}, \code{se} and \code{standard_label}.
}
\description{
\code{fit$quantities} uses the model's own abbreviated names. This returns the
table mapping each one to what it means, the units it is in, how it is
shaped, whether it carries a standard error, and what the same quantity is
called in the NOAA standardized assessment output.
}
\details{
Units follow the model throughout: numbers-at-age are \strong{thousands of fish}
and weight-at-age is \strong{kg}, so their product is \strong{mt}. A quantity whose
units read \code{"mt or thousands of fish"} follows its fleet's
\code{Observation_units} column.

\code{se = TRUE} means the TMB template \code{ADREPORT}s the quantity, so \code{fit$sdrep}
carries a standard error for it and \code{\link[=as.data.frame.Rceattle]{as.data.frame.Rceattle()}} can fill \code{se},
\code{lwr} and \code{upr}. \code{se = FALSE} means no standard error exists anywhere on the
fit for that quantity. Nothing has a standard error when the fit was produced
with \code{fit_control(getsd = FALSE)}, which leaves \code{sdrep} NULL.

\code{standard_label} gives the name the quantity takes in the NOAA standardized
assessment output that \code{stockplotr} and \code{asar} consume, so a table can be
relabelled for those tools; \code{NA} marks a quantity that standard has no
equivalent for. Rceattle's own names are kept as the primary vocabulary
because several quantities (predation, multispecies) have no standard name.

Every per-recruit reference point (\code{SPR0}, \code{SPRlimit}, \code{SPRtarget},
\code{SPRFinit}, \code{NbyageSPR}) is computed only under \code{msmMode = 0} and is exactly
\strong{zero on a multispecies fit} -- M there carries predation mortality, which
scales with predator abundance, so spawning output per recruit is not a
property of the prey stock alone.
}
\examples{
# The whole table
head(quantity_dictionary())

# What is ssb_depletion, and what units is it in?
quantity_dictionary("ssb_depletion")

# Everything that carries a standard error
dict <- quantity_dictionary()
dict[dict$se, c("quantity", "meaning")]

# Every reference point
quantity_dictionary(process = "reference_points")

# Search the meanings by keyword
dict[grep("depletion", dict$meaning, ignore.case = TRUE), ]
}
\seealso{
\code{\link[=parameter_dictionary]{parameter_dictionary()}} for the estimated parameters,
\code{\link[=as.data.frame.Rceattle]{as.data.frame.Rceattle()}} for these quantities in tidy form, and
\code{\link[=fit_mod]{fit_mod()}} for the fitted object they come from.
}
