% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-build_linkage.R
\name{linkage_spec}
\alias{linkage_spec}
\title{Capture a linkage specification}
\usage{
linkage_spec(
  formula,
  param = NULL,
  data = NULL,
  by = ~species,
  species = NULL,
  sex = NULL,
  link = "identity",
  init = NULL,
  bounds = NULL,
  priors = NULL,
  re_group = NA_character_,
  est_phase = 1L
)
}
\arguments{
\item{formula}{one-sided R formula whose RHS describes the linear
predictor for \code{param} (e.g. \code{~ 1}, \code{~ temp}, \code{~ temp + PDO}).}

\item{param}{target parameter name on the linear predictor scale
(e.g. \code{"log_alpha"}, \code{"log_M1"}, \code{"log_K"}). May be \code{NULL} when the
spec is built inside a \verb{build_*()} call that infers the parameter
name from the enclosing list key (see \code{\link[=build_growth]{build_growth()}}).}

\item{data}{(Optional) data frame for formula validation. Currently
validation happens at materialization time inside \code{\link[=fit_mod]{fit_mod()}}.}

\item{by}{one-sided formula naming stratifying factors that should
each get their own coefficients. Allowed names are \code{species},
\code{sex}, and \code{age_bin}. The default \code{~species} produces one
coefficient set per species (the typical multispecies
assessment use case); pass \code{~species + sex} for per-(species,
sex) coefficients, or \code{NULL} to share a single coefficient
set across every species/sex.}

\item{species}{optional integer vector of 1-based species ids that
this spec applies to. \code{NULL} (default) means every species in
\code{strata$species} at materialization time. Use this to give
different species different formulas, e.g. by registering
multiple specs against the same parameter -- see
\code{\link[=build_growth]{build_growth()}} for the multi-spec syntax.}

\item{sex}{optional vector of sex ids that this spec applies to.
May be supplied as integers (\code{1L} = female, \code{2L} = male) or as
character strings (\code{"Females"}/\code{"Males"}, case-insensitive;
\code{"female"}, \code{"male"}, \code{"f"}, \code{"m"} are also accepted). \code{NULL}
(default) means every sex in \code{strata$sex} at materialization
time. Only meaningful when \code{by} includes \code{sex}; otherwise the
filter is a no-op. Use this to register separate specs per sex
(e.g. one prior on females, another on males) against the same
parameter.}

\item{link}{link function applied to the predictor when assembling
process values; one of \link{LINKAGE_LINKS}. TODO}

\item{init}{optional named numeric vector of initial values keyed by
the design-matrix column name (e.g.
\verb{c(}(Intercept)\verb{ = -1, temp = 0)}). Missing entries default to \code{0}.}

\item{bounds}{optional named list of \code{c(lower, upper)} keyed the same
way as \code{init}.}

\item{priors}{optional named list whose entries are
\link{Rceattle_priors} objects, keyed by design-matrix column name.
Inside this argument the unprefixed shorthand \code{normal()},
\code{lognormal()}, \code{gamma()}, and \code{beta()} resolves to the
corresponding \verb{prior_*} constructors via a private data mask, so
\code{priors = list(temp = normal(0, 1))} works without masking
\code{\link[base:Special]{base::gamma()}} or \code{\link[base:Special]{base::beta()}} at the package level. Equivalent
to \code{priors = list(temp = prior_normal(0, 1))}.}

\item{re_group}{optional character: name of a random-effect grouping
for these coefficients. \code{NA} (default) means fixed.}

\item{est_phase}{optional integer estimation phase. Default \code{1L}.}
}
\value{
An \code{Rceattle_linkage_spec} object.
}
\description{
Capture a linkage specification
}
