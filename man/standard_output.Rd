% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/6-report_tables.R
\name{standard_output}
\alias{standard_output}
\title{Rceattle output in the NOAA standardized assessment format}
\usage{
standard_output(x, species = NULL, model = NULL)
}
\arguments{
\item{x}{An \code{"rceattle_report"} from \code{\link[=report_tables]{report_tables()}}, or an Rceattle fit,
which is passed through \code{\link[=report_tables]{report_tables()}} first.}

\item{species}{Which stock to emit: a species name, or an index into the
species this report holds, required when it holds more than one.}

\item{model}{Which model to emit when \code{x} holds several, defaulting to the
first.}
}
\value{
A data frame with the standardized columns, in the standard's own
order, plus \code{species} and \code{model}.
}
\description{
Relabels \code{\link[=report_tables]{report_tables()}} output into the standardized
assessment output that the \code{stockplotr} and \code{asar} packages consume, so
Rceattle results can be plotted and written into a report by the same tools
used for SS3, BAM, WHAM and FIMS.
}
\details{
Quantity names are translated through the \code{standard_label} column of
\code{\link[=quantity_dictionary]{quantity_dictionary()}}, so \code{ssb} becomes \code{spawning_biomass}, \code{R} becomes
\code{recruitment}, \code{F_spp} becomes \code{fishing_mortality}, and so on. A quantity the
standard has no name for keeps its Rceattle name, so nothing is silently
dropped.

\strong{The standard has no species dimension.} It describes one stock, so a
multispecies CEATTLE fit cannot be represented in it as a whole. A \code{species}
column is carried alongside the standard columns and \code{species} selects one
stock; with several species in the fit and no selection, this errors rather
than returning a frame in which two stocks' biomass share a year.

\code{era} follows the standard's own vocabulary: \code{"time"} for the hindcast the
model was fit to, \code{"fore"} for the projection.
}
\examples{
\dontrun{
fit <- fit_mod(data_list = BS2017SS, estimateMode = 1)
std <- standard_output(fit, species = 1)
# ...then, with the stockplotr package installed:
# stockplotr::plot_spawning_biomass(std)
}
}
\seealso{
\code{\link[=report_tables]{report_tables()}} for the native tables and
\code{\link[=quantity_dictionary]{quantity_dictionary()}} for the name crosswalk.
}
