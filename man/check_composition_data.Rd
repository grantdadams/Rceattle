% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/5-rearrange_data.R
\name{check_composition_data}
\alias{check_composition_data}
\title{Check and Clean Composition Data}
\usage{
check_composition_data(data_list)
}
\arguments{
\item{data_list}{A list containing the following components:
\itemize{
\item comp_obs: A matrix or data frame of composition observations.
\item comp_data: A data frame with metadata including species, sex, and age/length information.
\item nsex: A vector of sex counts by species.
\item nages: A vector of age counts by species.
\item nlengths: A vector of length counts by species.
}}
}
\value{
The modified \code{data_list} with NA values in \code{comp_obs} converted to 0.
}
\description{
This function checks the composition data for zero sum rows, handles NA values,
and verifies that the composition data spans the required range of ages/lengths.
}
\examples{
# Example usage:
data_list <- list(
  comp_obs = matrix(c(1, 2, 3, 0, 4, 5), nrow = 2),
  comp_data = data.frame(Species = c(1, 2), Sex = c(1, 3), Age0_Length1 = c(0, 1)),
  nsex = c(1, 2),
  nages = c(5, 6),
  nlengths = c(10, 12)
)
cleaned_data_list <- check_composition_data(data_list)

}
