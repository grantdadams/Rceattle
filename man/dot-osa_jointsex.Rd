% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7-plot_osa.R
\name{.osa_jointsex}
\alias{.osa_jointsex}
\title{Split joint-sex (Sex == 3) composition bins onto a single age/length axis}
\usage{
.osa_jointsex(df, nages, nlengths)
}
\arguments{
\item{df}{A data frame with \code{species}, \code{sex}, \code{index_label}, \code{age_length_bin},
and \code{source} columns.}

\item{nages, nlengths}{Per-species bin counts (or \code{NULL} to skip the split).}
}
\description{
Joint-sex compositions stack females in bins \verb{1..nbin} and males in bins
\verb{nbin+1..2*nbin} (where \code{nbin} is \code{nages} or \code{nlengths} for the species).
This re-bases the male bins to \verb{1..nbin} and tags the source label by sex so
males and females face the same bin axis -- matching \code{\link[=plot_comp]{plot_comp()}}. Rows with
Sex != 3 (single-sex or combined) are returned unchanged.
}
\keyword{internal}
