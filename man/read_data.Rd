% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-read_write_excel_data.R
\name{read_data}
\alias{read_data}
\title{Read a CEATTLE excel data file}
\usage{
read_data(file = "Rceattle_data.xlsx")
}
\arguments{
\item{file}{Filname to be used. Must end with '.xlsx'}
}
\description{
Read a CEATTLE excel data file
}
\examples{

library(Rceattle)
data(BS2017SS)
out_file <- file.path(tempdir(), "BS2017SS.xlsx")
write_data(data_list = BS2017SS, file = out_file)
data_list <- read_data(file = out_file)
# Or read + override blocks in one call:
data_list <- build_data(file = out_file, projyr = 2060)
file.remove(out_file)
}
\seealso{
\code{\link[=build_data]{build_data()}} to assemble or edit a data list in R (and to read a
workbook then override blocks in one call, \code{build_data(file = ...)}),
\code{\link[=data_requirements]{data_requirements()}} to see which inputs a configuration needs.
}
