% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-rceattle_class.R
\name{as.data.frame.Rceattle}
\alias{as.data.frame.Rceattle}
\title{Tidy long-format derived quantities from an Rceattle fit}
\usage{
\method{as.data.frame}{Rceattle}(
  x,
  row.names = NULL,
  optional = FALSE,
  which = c("biomass", "ssb", "R", "biomass_depletion", "ssb_depletion", "F_spp"),
  ci_level = 0.95,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"Rceattle"} returned by \code{\link[=fit_mod]{fit_mod()}}.}

\item{row.names, optional}{Ignored; present for the \code{\link[=as.data.frame]{as.data.frame()}} generic.}

\item{which}{Character vector of quantity names to extract, or
\code{"all"} for every quantity with a known shape. Defaults to a common
population-level summary. See \code{names(Rceattle:::.RCEATTLE_QUANTITIES)}
for the full list.}

\item{ci_level}{Confidence level for \code{lwr} and \code{upr}. Default \code{0.95}.}

\item{...}{Currently unused.}
}
\value{
A \code{data.frame} with columns \code{year}, \code{species}, \code{sex}, \code{age},
\code{quantity}, \code{value}, \code{se}, \code{lwr}, \code{upr}. \code{species} is the character
species name from \code{data_list$spnames}. Rows are sorted in the order
\code{which} was given.
}
\description{
Returns the model's derived population quantities in long form so that
custom plots and post-processing don't have to walk the deeply nested
\code{quantities} list or inherit the dimnames decisions in
\code{\link[=rename_output]{rename_output()}}. Two shapes are flattened into one tidy frame:
species-by-year quantities (e.g. \code{biomass}, \code{ssb}, \code{R}, \code{F_spp}) and
species-by-sex-by-age-by-year quantities (e.g. \code{N_at_age},
\code{biomass_at_age}, \code{M_at_age}). For the species-level shapes, \code{sex}
and \code{age} are returned as \code{NA}. Cells of the 4D arrays that are
padded out to \code{max(nsex)} / \code{max(nages)} for species with fewer
sexes or ages are dropped.
}
\details{
Standard errors (\code{se}) and confidence intervals (\code{lwr}, \code{upr}) are
populated from the TMB \code{sdreport} for any quantity that was
\code{ADREPORT}'d (\code{biomass}, \code{ssb}, \code{R}, \code{exploitable_biomass},
\code{biomass_depletion}, \code{ssb_depletion}); other quantities and fits produced
with \code{getsd = FALSE} get \code{NA} for \code{se} / \code{lwr} / \code{upr}. Set \code{ci_level} to
widen or narrow the band.

A strictly positive series takes its interval on the log scale, so it is
right-skewed and cannot reach zero. This is the same construction
\code{\link[=plot_timeseries]{plot_timeseries()}} draws, so the table and the figure agree.
}
