% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osa_residuals.R
\name{.osa_build_obj}
\alias{.osa_build_obj}
\title{Rebuild a fitted Rceattle TMB object in OSA mode at the fitted parameters}
\usage{
.osa_build_obj(fit)
}
\arguments{
\item{fit}{A fitted \code{Rceattle} object.}
}
\value{
A TMB ADFun object with \code{osa_mode = 1}.
}
\description{
Returns a TMB ADFun object equivalent to \code{fit$obj} but with \code{osa_mode = 1},
built at the fitted parameter values and the same map / random-effect
structure, ready for \code{\link[TMB:oneStepPredict]{TMB::oneStepPredict()}}. In OSA mode the composition
likelihoods read their counts from \code{obsvec} and use unweighted proper
densities; the aggregate catch/index likelihood is identical in both modes,
so this single object serves every observation type.
}
\keyword{internal}
