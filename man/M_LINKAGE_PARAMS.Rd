% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0-build_srr_and_M.R
\docType{data}
\name{M_linkage_params}
\alias{M_linkage_params}
\alias{M_LINKAGE_PARAMS}
\title{Define M1 specifications}
\format{
An object of class \code{character} of length 1.
}
\usage{
M_LINKAGE_PARAMS
}
\arguments{
\item{M1_model}{Vector or scalar specifying M1 fixed effects model (see @description below). 0 = use fixed natural mortality from M1_base in data, 1 = estimate sex- and age-invariant M1, 2 = sex-specific (two-sex model), age-invariant M1, 3 = estimate sex- and age-specific M1, 4 = environmentally driven sex- and age-invariant M1, 5 = environmentally driven age-invariant, but sex-specific M1.}

\item{M1_re}{Vector or scalar specifying M1 random effects model. See description (default = 0).}

\item{updateM1}{If using initial parameters, use M1 fixed effects from data instead (default = FALSE).}

\item{M1_use_prior}{Vector or scalar specifying if M1 fixed effects come from a lognormal prior}

\item{M2_use_prior}{Vector or scalar specifying if M1 + M2 come from a lognormal prior in multi-species models (default = FALSE). Lognormal prior for M1 + M2 across species, sexes, ages, and years.}

\item{M_prior}{Vector or scalar for mean of M prior on natural scale}

\item{M_prior_sd}{Vector or scalar of SD of lognormal M prior. Used as initial value for random effects variance as well.}

\item{M1_indices}{vector or single index indicating the columns (excluding Year column) of \code{env_data} to use for environmentally linked M1 when \code{M1_model} is 4 or 5.}
}
\description{
\strong{M1 fixed effects currently implemented in CEATTLE}
\itemize{
\item \code{M1_model = 0} or \code{"fixed"}: Fixed based on input \code{M1_base}
\item \code{M1_model = 1} or \code{"sex_age_invariant"}: Single species specific M. Estimates \deqn{M1_{spp}}
\item \code{M1_model = 2} or \code{"sex_specific"}: Sex-specific M. Estimates \deqn{M1_{spp, sex}}
\item \code{M1_model = 3} or \code{"sex_age_specific"}: Sex- and age-specific M. Estimates \deqn{M1_{spp, sex, age}}
\item \code{M1_model = 4}, \code{5}: Soft-deprecated env-driven codes; use the \code{linkages} argument instead. See \code{vignette("environmental-linkages")}.
}

\strong{M1 random effects currently implemented in CEATTLE}

M1 random effects are applied to each species if \code{M1_model = 1} or each species and sex if \code{M1_model = 2}. Variance and correlation coefficients are species-specific, but sex-invariant.
\itemize{
\item \code{M1_re = 0} or \code{"none"}: No random effects (default).
\item \code{M1_re = 1} or \code{"iid_age"}: Random effects varies by age, but uncorrelated (IID) and constant over years.
\item \code{M1_re = 2} or \code{"iid_year"}: Random effects varies by year, but uncorrelated (IID) and constant over ages.
\item \code{M1_re = 3} or \code{"iid_age_year"}: Random effects varies by year and age, but uncorrelated (IID).
\item \code{M1_re = 4} or \code{"ar1_age"}: Correlated AR1 random effects varies by age, but constant over years.
\item \code{M1_re = 5} or \code{"ar1_year"}: Correlated AR1 random effects varies by year, but constant over ages.
\item \code{M1_re = 6} or \code{"ar1_age_year"}: Correlated 2D-AR1 random effects varies by year and age.
}

Allowed M-parameter names for \code{linkages} in \code{\link[=build_M1]{build_M1()}}.
Linear-predictor names of the underlying natural-mortality
parameters that the linkage system can address. Currently just
\code{log_M1} -- the offset is added on the log scale to \code{ln_M1}
(broadcast across age unless the linkage row pins a specific
\code{age_bin}).

Note: predation mortality \code{M2} is a derived quantity in CEATTLE
(a function of predator abundance, suitability, and ration), not
a parameter. There is no \code{log_M2} linkage target; environmental
effects on predation are mediated upstream via recruitment,
growth, suitability, or ration inputs.
}
\keyword{internal}
